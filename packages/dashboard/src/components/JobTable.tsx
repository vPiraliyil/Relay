import type { Job } from '../../../../shared/src/types';

/**
 * Displays a table of jobs with columns for id, type, state, attempts, and createdAt.
 * Supports filtering by state.
 */
export function JobTable(): React.JSX.Element {
  // TODO: accept jobs as props or fetch from context
  // TODO: render a table with job data
  // TODO: add state filter controls

  return (
    <section>
      {/* TODO: render job table */}
    </section>
  );
}
