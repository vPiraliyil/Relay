import type { QueueStats } from '../../../../shared/src/types';

/**
 * Displays queue statistics — pending, active, completed, failed, dlq counts.
 */
export function StatsBar(): React.JSX.Element {
  // TODO: accept stats as props or fetch from context
  // TODO: render each stat as a card/badge

  return (
    <section>
      {/* TODO: render QueueStats */}
    </section>
  );
}
